import React from 'react';

/**
 * Live chat button component - now disabled for all users
 * Email support is the primary support channel with plan-specific response times
 */
export const LiveChatButton: React.FC = () => {
  // Live chat feature has been removed - all support is now handled via email
  // with different response times based on user plan:
  // - Free: 48-72 hours
  // - Premium: 24-48 hours  
  // - Pro: 4 hours
  // - Lifetime: 4 hours
  
  return null;
};